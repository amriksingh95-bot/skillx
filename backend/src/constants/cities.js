/**
 * Canonical city definitions.
 * Single source of truth for both backend and frontend.
 *
 * State is always Punjab — no dropdown needed.
 */

const STATE = 'Punjab';

const CITIES = ['Ludhiana', 'Jalandhar', 'Amritsar'];

const CITY_VALUES = CITIES;

module.exports = { STATE, CITIES, CITY_VALUES };
